{
  "kind": "collectionType",
  "collectionName": "contact_submissions",
  "info": {
    "singularName": "contact-submission",
    "pluralName": "contact-submissions",
    "displayName": "Contact Submission",
    "description": "Заявки из контактной формы"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "email": {
      "type": "email",
      "required": true
    },
    "services": {
      "type": "json",
      "required": true
    },
    "status": {
      "type": "enumeration",
      "enum": ["new", "processing", "completed", "rejected"],
      "default": "new",
      "required": true
    },
    "notes": {
      "type": "richtext"
    }
  }
}
